# 权限控制和路由守卫实现原理。

1. **权限控制的核心机制**：

   - 使用 Pinia store 管理权限状态（`permissionStore`）
   - 支持三种路由生成模式：
     - `server`：后端动态返回路由配置
     - `frontEnd`：前端根据权限过滤路由
     - `static`：静态路由表

2. **路由守卫实现（`permission.ts`）**：

```typescript
router.beforeEach(async (to, from, next) => {
  // 1. 检查用户是否已登录
  if (userStore.getUserInfo) {
    // 2. 如果访问登录页，直接跳转到首页
    if (to.path === '/login') {
      next({ path: '/' })
    } else {
      // 3. 检查是否已添加动态路由
      if (permissionStore.getIsAddRouters) {
        next()
        return
      }

      // 4. 获取用户角色对应的路由权限
      const roleRouters = userStore.getRoleRouters || []

      // 5. 根据配置生成动态路由
      if (appStore.getDynamicRouter) {
        // 可以选择使用服务端或前端方式生成路由
        await permissionStore.generateRoutes(...)
      }

      // 6. 动态添加路由
      permissionStore.getAddRouters.forEach((route) => {
        router.addRoute(route)
      })

      // 7. 设置路由已添加标志
      permissionStore.setIsAddRouters(true)
      next({ ...to, replace: true })
    }
  } else {
    // 8. 未登录时处理
    if (在白名单中) {
      next()
    } else {
      next(`/login?redirect=${to.path}`)
    }
  }
})
```

3. **路由生成机制**：

a) **前端过滤模式**（`generateRoutesByFrontEnd`）：

```typescript
export const generateRoutesByFrontEnd = (routes, keys, basePath = '/') => {
  // 1. 遍历路由配置
  // 2. 根据用户权限keys过滤路由
  // 3. 递归处理子路由
  // 4. 返回过滤后的路由表
}
```

b) **后端控制模式**（`generateRoutesByServer`）：

```typescript
export const generateRoutesByServer = (routes) => {
  // 1. 遍历后端返回的路由配置
  // 2. 动态加载组件
  // 3. 递归处理子路由
  // 4. 返回处理后的路由表
}
```

4. **权限验证流程**：

   - 用户登录后获取角色信息和权限列表
   - 根据权限列表过滤路由配置
   - 动态添加过滤后的路由
   - 用户只能访问其权限范围内的路由

5. **路由数据持久化**：

```typescript
persist: {
  paths: ['routers', 'addRouters', 'menuTabRouters']
}
```

6. **特殊处理**：
   - 支持多级路由降级（`flatMultiLevelRoutes`）
   - 自动处理404路由
   - 支持路由组件的动态加载
   - 支持路由重定向和面包屑

这样的设计实现了：

1. 不同用户看到不同的菜单
2. 用户只能访问其权限内的路由
3. 灵活的路由生成方式（前端/后端/静态）
4. 良好的用户体验（动态加载、持久化）
5. 完善的安全控制（路由守卫、权限验证）
